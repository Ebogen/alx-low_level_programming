#!/bin/bash
#include "main.h"

/**
 * _strspn - a function that rep strspn
 * @s: a pointer to a string
 * @accept: a pointer to a character
 * Return: nothing
 */

unsigned int _strspn(char *s, char *accept)
{



}
