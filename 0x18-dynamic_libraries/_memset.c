#!/bin/bash
#include "main.h"

/**
 * _memset - a function
 * @s: a pointer to a string
 * @b: a character
 * @n: an unsigned int
 * Return: nothing
 */

char *_memset(char *s, char b, unsigned int n)
{



}
