#include <stdio.h>

/**
 * main - main function
 * @argc: parameter count
 * @argv: parameter pointer
 * Return: always 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
